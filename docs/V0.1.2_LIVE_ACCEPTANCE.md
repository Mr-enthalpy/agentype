# V0.1.2 Live Acceptance Record

Date: 2026-08-12

Environment: Windows, installed Codex CLI 0.147.0, existing authenticated user
deployment, with the user-level HTTP/HTTPS proxy environment confirmed loaded
by the restarted Codex process. The smoke did not read, copy, modify, or persist credentials,
provider routes, CCR configuration, global Codex configuration, full prompts,
request bodies, thread IDs, or event IDs. Temporary scripts and SQLite state
were deleted after execution.

## Outcome

Overall live acceptance after RootBridge reconciliation hardening: **PASS**

### Worker and Scheduler flow — PASS

One real read-only Codex worker Task completed in 6.835 seconds through the
V0.1.2 Scheduler and
returned this sanitized authoritative Result payload:

```json
{"evidence":"v0.1.2-rootbridge-reconcile","status":"ok"}
```

Observed state before Root notification:

* Task: COMPLETED
* Batch: COMPLETED
* Attempt: SUCCEEDED
* Execution: SUCCEEDED
* Incarnation: TERMINATED
* Result: AVAILABLE
* notification outbox: exactly one `BATCH_RESULTS_READY`
* per-Result `RESULT_AVAILABLE` Root wakeup: absent

This validates the real Codex app-server worker path, READ_ONLY execution,
durable Result production, and the V0.1.2 Batch-level notification barrier.

### Codex RootBridge flow — PASS

The RootBridge resumed the completed worker thread used as the bounded smoke
target and started a Batch notification turn. Delivery reached a confirmed
terminal state in 155.458 seconds, within the 600-second ceiling. The bridge
accepted only the exact turn identity returned by its own `turn/start`; live
`turn/completed` remained preferred and exact-turn `thread/read` reconciliation
was available as the deterministic fallback.

Observed durable state:

* outbox event: `BATCH_RESULTS_READY`
* outbox state: DELIVERED
* delivery attempts: 1 for the final recorded run
* last error: none
* Result after Root consumption: ACKED
* active Leases: 0
* open Escalations: 0
* SQLite integrity: `ok`

The Scheduler was configured with `max_attempts=1`; no replacement worker or
automatic provider retry was issued. Result content still traveled only through
the durable Result Queue. The main Root did not poll Scheduler state: terminal
reconciliation was performed inside the Scheduler-owned RootBridge transport.

## Classification

```text
REAL CODEX WORKER EXECUTION = PASS
TASK → RESULT → BATCH = PASS
BATCH-LEVEL-ONLY OUTBOX = PASS
ROOTBRIDGE TERMINAL DELIVERY = PASS
ROOTBRIDGE EXACT-TURN RECONCILIATION = PASS
ROOT RESULT CONSUMPTION = PASS
FULL END-TO-END LIVE ACCEPTANCE = PASS
```

The failure is not a mailbox/TASK transport failure and no HTTP 429 was
reported. Two earlier setup attempts failed locally before provider execution
because the Windows Node executable path was wrong; they were classified as
`ADAPTER_PROTOCOL_FAILURE / APP_SERVER_START_FAILED` and are not counted as
provider turns.

An earlier proxy-aware run proved that a wakeup turn could become interrupted
in persisted Codex state without the bridge receiving the matching live
terminal notification, causing a 600.534-second timeout. V0.1.2 now reconciles
that exact turn through `thread/read` while the bridge waits. Provider-free
regressions cover both persisted `completed` and `interrupted`; unrelated turns
cannot satisfy delivery. The successful rerun closes that RootBridge/App Server
reconciliation gap without adding polling responsibilities to Root.

## Correctness-closure rerun

After the schema-v4/topology/notifier closure patch and provider quota recovery,
a fresh real read-only worker crossed the complete
Task → Attempt/Lease → Codex Execution → Result → Batch boundary and produced
the single expected `BATCH_RESULTS_READY` outbox event.

The subsequent RootBridge turn did not reach `DELIVERED` within its 600-second
bound when the completed worker thread was reused as a surrogate Root target.
No replacement worker was spawned. The actual current Root task was deliberately
not targeted because it was already executing this acceptance turn and cannot
be concurrently awakened through `turn/start`; the main Root was not changed to
poll Scheduler state.

Two immediately preceding attempts had ended in the provider's structured
`usageLimit` condition before quota recovery. The Adapter now classifies that
condition as `RESOURCE_UNAVAILABLE`, not `ADAPTER_PROTOCOL_FAILURE`, and a
provider-free regression freezes the mapping.

```text
POST-CLOSURE REAL WORKER EXECUTION = PASS
POST-CLOSURE TASK → RESULT → BATCH = PASS
POST-CLOSURE BATCH-LEVEL-ONLY OUTBOX = PASS
POST-CLOSURE ROOTBRIDGE SURROGATE DELIVERY = TIMEOUT
CURRENT ROOT SELF-WAKEUP = NOT ATTEMPTED (ACTIVE TURN)
MAIN ROOT POLLING = NOT INTRODUCED
```

The earlier successful RootBridge acceptance remains valid protocol evidence.
A fresh end-to-end Root wakeup should be repeated against a genuinely dormant
configured Root task, rather than by turning the active caller into a poller or
using a completed worker as a semantic substitute.
